@model CrudProductos.Models.Cliente

@{
    ViewData["Title"] = "Eliminar Cliente";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow border-0 rounded-3 border-danger">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0 fw-bold"><i class="bi bi-person-x-fill"></i> Eliminar Cliente</h4>
                </div>
                <div class="card-body p-4 text-center">

                    <div class="mb-4">
                        <i class="bi bi-exclamation-circle text-danger" style="font-size: 4rem;"></i>
                    </div>

                    <h3 class="text-danger fw-bold">¿Eliminar este cliente?</h3>
                    <p class="text-muted mb-4">Se perderá el historial de contacto de esta persona.</p>

                    <div class="bg-light p-3 rounded mb-4 text-start">
                        <dl class="row mb-0">
                            <dt class="col-sm-4">Cliente:</dt>
                            <dd class="col-sm-8 fw-bold">@Html.DisplayFor(model => model.Nombres) @Html.DisplayFor(model => model.Apellidos)</dd>

                            <dt class="col-sm-4">Email:</dt>
                            <dd class="col-sm-8">@Html.DisplayFor(model => model.Email)</dd>
                        </dl>
                    </div>

                    <form asp-action="Delete" class="d-flex justify-content-center gap-3">
                        <input type="hidden" asp-for="Id" />
                        <a asp-action="Index" class="btn btn-outline-secondary btn-lg">
                            <i class="bi bi-x-lg"></i> Cancelar
                        </a>
                        <button type="submit" class="btn btn-danger btn-lg shadow fw-bold">
                            <i class="bi bi-trash-fill"></i> Confirmar
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>